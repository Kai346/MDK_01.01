# Векторы и строки в C++
## Основные понятия
> Вектор (std::vector) и строка (std::string) — базовые контейнеры стандартной библиотеки C++.
## Общие характеристики:
- Хранят элементы в непрерывном фрагменте памяти
- Предоставляют доступ по индексу (как массивы)
- Позволяют эффективно добавлять элементы в конец
- Автоматически управляют памятью (нет утечек)
# Контейнер std::vector
## Что такое вектор?
> Динамический массив, который:
- Меняет свой размер при необходимости
- Обеспечивает быстрое добавление в конец (O(1) в среднем)
- Гарантирует отсутствие утечек памяти
  
> #include <vector>  // не <vector.h>!
## Типизация
